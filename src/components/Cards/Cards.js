import React, { Component } from 'react';
import './Cards.css';

class Cards extends Component {
    render() { 
        return (
            <ul>
            {/* {this.props.items.map(item => (
              <li key={item.id}>{item.block}</li>
            ))} */}
          </ul>
        );
    }
}
 
export default Cards;